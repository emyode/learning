<Page
    x:Class="Blinky.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Blinky"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBox x:Name="CommandTextBox" />
            <Button x:Name="GO" Content="GO" Click="GO_Click" />
            <Image x:Name="image" HorizontalAlignment="Center" Height="231" Margin="292,10,0,0" VerticalAlignment="Bottom" Width="450"/>
            <ScrollViewer x:Name="MessageScroll">
                <TextBlock Height="100" x:Name="MessagesTextBlock" />
            </ScrollViewer>
            <TextBox x:Name="MessagesTextBox" Height="100" TextWrapping="Wrap" ScrollViewer.VerticalScrollBarVisibility="Visible" ScrollViewer.VerticalScrollMode="Enabled" AcceptsReturn="True" />
        </StackPanel>
    </Grid>
</Page>
